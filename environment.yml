# ``conda env create --file environment.yml``
# or
# ``conda env create --name <myEnvName> --file environment.yml``

name: pastis
dependencies:
  - astropy
  - configparser
  - jupyter
  - matplotlib
  - numpy
  - pandas
  - pip
  - conda-forge::pypdf2
  - pytest
  - python=3.7
  - scipy
  - pip:
    - fpdf
    - poppy
    - hcipy
